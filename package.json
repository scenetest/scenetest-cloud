{
  "name": "scenetest-cloud",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "concurrently -k -n worker,dashboard -c blue,magenta \"pnpm dev:worker\" \"pnpm dev:dashboard:watch\"",
    "dev:worker": "node -e \"fs.mkdirSync('dist/dashboard',{recursive:true})\" && wrangler dev",
    "dev:dashboard": "vite",
    "dev:dashboard:watch": "vite build --watch",
    "build:dashboard": "vite build",
    "deploy": "node scripts/encode-assets.mjs && pnpm build:dashboard && wrangler deploy --env production",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "e2e": "node scripts/e2e.mjs"
  },
  "dependencies": {
    "@scenetest/dashboard": "^0.13.0",
    "@scenetest/protocol": "^0.11.0",
    "@tailwindcss/vite": "^4.3.0",
    "@tanstack/react-query": "^5.101.0",
    "partysocket": "^1.2.0",
    "preact": "^10.29.2",
    "preact-iso": "^2.12.0",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260610.1",
    "concurrently": "^10.0.3",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8",
    "wrangler": "^4.99.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd"
    ]
  }
}
